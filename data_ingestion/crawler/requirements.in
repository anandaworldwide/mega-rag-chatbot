# Legacy reference file for crawler dependencies.
# The source of truth is now `data_ingestion/crawler/pyproject.toml` + the shared `uv.lock`.
# Compatibility `requirements.txt` exports are generated via `bin/export-python-requirements.sh`.

# Web crawling
beautifulsoup4
playwright
readability-lxml

# Environment and config
python-dotenv

# OpenAI embeddings
langchain-openai
langchain-core>=1.2.11  # Includes latest langchain-core security fixes.

# Vector database
pinecone

# Text chunking (spaCy for sentence boundaries)
spacy

# Token counting
tiktoken

# Progress bars
tqdm

# System monitoring (optional but useful)
psutil

# AWS SDK for CloudWatch logs access
boto3

# Security fixes
orjson>=3.11.6  # CVE-2025-67221: recursion DoS fix
requests>=2.33.0  # CVE-2026-25645
urllib3>=2.6.3  # CVE-2025-66418, CVE-2025-66471, CVE-2026-21441: Decompression bomb fixes
