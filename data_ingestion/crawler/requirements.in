# Crawler-specific requirements (minimal)
# pip-compile requirements.in to generate requirements.txt

# Web crawling
beautifulsoup4
playwright
readability-lxml

# Environment and config
python-dotenv

# OpenAI embeddings
langchain-openai
langchain-core>=1.2.5  # CVE-2025-68664: Serialization injection fix (langchain-openai 1.1.7 pulls in 1.1.3, so pin directly)

# Vector database
pinecone

# Text chunking (spaCy for sentence boundaries)
spacy

# Token counting
tiktoken

# Progress bars
tqdm

# System monitoring (optional but useful)
psutil

# AWS SDK for CloudWatch logs access
boto3
