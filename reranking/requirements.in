# Legacy reference file for reranking dependencies.
# The source of truth is now `reranking/pyproject.toml` + the shared `uv.lock`.
# Compatibility `requirements.txt` exports are generated via `bin/export-python-requirements.sh`.

transformers>=4.53.0  # CVE-2025-5197, CVE-2025-3933: ReDoS fixes
optimum[onnxruntime]>=1.26.1  # Required for transformers 4.53+ compatibility
torch>=2.13.0,<3.0  # Pinned to 2.2.x: PyTorch >=2.6.0 doesn't publish wheels for Python 3.12.
                   # Known CVEs accepted until Python upgrade. Reranking not actively used.
onnx>=1.22.0  # CVE-2026-44512 + CVE-2026-28500 + CVE-2026-27489, CVE-2026-34445/34446/34447, GHSA-q56x-g2fj-4rj6
filelock>=3.20.1  # CVE-2025-68146: TOCTOU symlink attack fix (transitive dependency needs pinning)
requests>=2.33.0  # CVE-2026-25645 (transitive dependency needs pinning)
urllib3>=2.7.0  # CVE-2025-66418, CVE-2025-66471, CVE-2026-21441: Decompression bomb fixes (transitive dependency needs pinning)
scikit-learn
numpy
protobuf>=6.33.5  # Includes latest protobuf security fixes.
