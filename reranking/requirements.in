# Dependencies for the reranking fine-tuning and evaluation scripts
# Compile using: pip-compile reranking/requirements.in -o reranking/requirements.txt
# Install using: pip install -r reranking/requirements.txt

transformers>=4.53.0  # CVE-2025-5197, CVE-2025-3933: ReDoS fixes
optimum[onnxruntime]>=1.26.1  # Required for transformers 4.53+ compatibility
torch>=2.2.0,<3.0  # Pinned to 2.2.x: PyTorch >=2.6.0 doesn't publish wheels for Python 3.12.
                   # Known CVEs accepted until Python upgrade. Reranking not actively used.
onnx>=1.19.1  # CVE-2026-28500: security control bypass fix
filelock>=3.20.1  # CVE-2025-68146: TOCTOU symlink attack fix (transitive dependency needs pinning)
urllib3>=2.6.3  # CVE-2025-66418, CVE-2025-66471, CVE-2026-21441: Decompression bomb fixes (transitive dependency needs pinning)
scikit-learn
numpy
protobuf>=6.33.5  # Includes latest protobuf security fixes.
